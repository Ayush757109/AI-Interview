/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "randomuser.me",
      "images.unsplash.com",
      "avatars.githubusercontent.com",
    ],
  },
};

export default nextConfig;